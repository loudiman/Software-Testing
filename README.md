# composer-phpunit-boilertemplate

<p>To run the phpunit in composer: </p>
```
  vendor\bin\phpunit [directory/file]
```

Note:
  - Add a postfix 'test' (e.g., ExampleTest) for every file in the tests directory
